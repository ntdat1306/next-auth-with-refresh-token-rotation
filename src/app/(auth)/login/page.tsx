import { PageContainer } from '@/components/ui/PageContainer';
import React from 'react';
import { LoginForm } from './_components/LoginForm';

const Login = () => {
    return (
        <PageContainer center='both'>
            <LoginForm />
        </PageContainer>
    );
};

export default Login;
